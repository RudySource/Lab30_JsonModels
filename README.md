# Лабораторная работа №30. JSON и модели данных

---

## Основная информация

**ФИО:** Rudy Rudy Rudy  
**Группа:** ИСП-233  
**Дата:** 24.08.2077

---

## Таблица всех маршрутов API

| Метод | Маршрут                 | Описание                         | Параметры                        |
| ----- | ----------------------- | -------------------------------- | -------------------------------- |
| GET   | `/api/heroes`           | Получить всех героев             | `?universe=Marvel` (опционально) |
| GET   | `/api/heroes/{id}`      | Получить героя по ID             | `id` — номер героя               |
| GET   | `/api/heroes/demo`      | Сравнение настроек сериализации  | Нету                             |
| GET   | `/api/heroes/serialize` | Демонстрация ручной сериализации | Нету                             |
| GET   | `/api/heroes/search`    | Поиск героев по имени            | `?name=паук`                     |

---

## Обязательная таблица атрибутов JSON:

| Атрибут/настройка                | Что делает              | Пример                           |
| -------------------------------- | ----------------------- | -------------------------------- |
| `[JsonPropertyName("name")]`     | Задаёт имя поля в JSON  | `"name"` вместо `"Name"`         |
| `[JsonIgnore]`                   | Поле не попадает в JSON | Пароли, внутренние поля          |
| `PropertyNamingPolicy.CamelCase` | Все поля в camelCase    | `"realName"` вместо `"RealName"` |
| `JsonStringEnumConverter`        | Enum как строка         | `"Marvel"` вместо `0`            |
| `WriteIndented = true`           | JSON с отсупами         | Для удобства при разработке      |

---

## Главные выводы — напишите своими словами:

1.  Что такое сериализация и десериализация?
    - **Сериализация** — преобразования объекта C# в JSON-строку.
    - **Десериализация** — reverse Сериализации: преобразование JSON-строки обратно в объект C#.

2.  Зачем нужен `[JsonIgnore]` — приведите реальный пример

- **Реальный пример**:

  ```csharp
  public class User
  {
    public int Id { get; set; }
    public string Name { get; set; }

    [JsonIgnore]
    public string Password { get; set; }

    public string Email { get; set; }
  }
  ```

- **Результат JSON:**:

  ```csharp
  {
  "id": 1,
  "name": "Rudy",
  "email": "rudy@mail.com"
  }
  ```

> [!NOTE]
> Поле `Password` отсутствует.

3. Почему enum лучше сериализовывать строкой а не числом 4. Что изменилось в ответе после настройки CamelCase
   - **Понятнее**: видно "Marvel", а не просто 0
   - **Надёжнее**: если поменять порядок в enum, числа сломаются, а строки — нет
     <br>
4. Что изменилось после `CamelCase`
   - Все поля теперь с маленькой буквы (camelCase)
   - universe стал "Marvel", а не 0
   - internalNotes убрали (из-за `[JsonIgnore]`)
   - `JSON` стал красивее, с отступами
